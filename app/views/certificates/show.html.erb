<div class="container mx-auto py-8">
  <% if @certificate.pdf.attached? %>
    <div class="bg-white rounded-lg shadow p-6 text-center">
      <h1 class="text-2xl font-bold mb-4">Certificado de Conclusão</h1>
      <p class="mb-4">Seu certificado está pronto para download.</p>
      <%= link_to 'Download do Certificado', download_certificate_path(@certificate),
            class: 'bg-blue-600 text-white px-6 py-3 rounded hover:bg-blue-700 inline-block' %>
    </div>
  <% else %>
    <div class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-4">
      <p>Seu certificado está sendo gerado. Por favor, atualize a página em alguns instantes.</p>
      <%= button_to 'Atualizar', certificate_path(@certificate), method: :get,
            class: 'mt-2 bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700' %>
    </div>
  <% end %>
</div>